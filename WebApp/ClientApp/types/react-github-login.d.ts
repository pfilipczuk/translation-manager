export default class _default {
    constructor(...args: any[]);
    forceUpdate(callback: any): void;
    render(): any;
    setState(partialState: any, callback: any): void;
}
export default namespace _default {
    namespace defaultProps {
        const buttonText: string;
        function onFailure(): void;
        function onRequest(): void;
        function onSuccess(): void;
        const scope: string;
    }
    namespace propTypes {
        function buttonText(e: any, t: any, n: any, r: any, u: any, c: any): void;
        namespace buttonText {
            // Circular reference from GitHubLogin.default.propTypes.buttonText
            const isRequired: any;
        }
        function children(e: any, t: any, n: any, r: any, u: any, c: any): void;
        namespace children {
            // Circular reference from GitHubLogin.default.propTypes.children
            const isRequired: any;
        }
        function className(e: any, t: any, n: any, r: any, u: any, c: any): void;
        namespace className {
            // Circular reference from GitHubLogin.default.propTypes.className
            const isRequired: any;
        }
        function clientId(e: any, t: any, n: any, r: any, u: any, c: any): void;
        namespace clientId {
            // Circular reference from GitHubLogin.default.propTypes.clientId
            const isRequired: any;
        }
        function onFailure(e: any, t: any, n: any, r: any, u: any, c: any): void;
        namespace onFailure {
            // Circular reference from GitHubLogin.default.propTypes.onFailure
            const isRequired: any;
        }
        function onRequest(e: any, t: any, n: any, r: any, u: any, c: any): void;
        namespace onRequest {
            // Circular reference from GitHubLogin.default.propTypes.onRequest
            const isRequired: any;
        }
        function onSuccess(e: any, t: any, n: any, r: any, u: any, c: any): void;
        namespace onSuccess {
            // Circular reference from GitHubLogin.default.propTypes.onSuccess
            const isRequired: any;
        }
        function redirectUri(e: any, t: any, n: any, r: any, u: any, c: any): void;
        namespace redirectUri {
            // Circular reference from GitHubLogin.default.propTypes.redirectUri
            const isRequired: any;
        }
        function scope(e: any, t: any, n: any, r: any, u: any, c: any): void;
        namespace scope {
            // Circular reference from GitHubLogin.default.propTypes.scope
            const isRequired: any;
        }
    }
}
